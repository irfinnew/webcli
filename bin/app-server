#! /bin/sh

set -e

export PATH="$HOME/bin:$PATH"

export CY_ENV_FILE="$HOME/.env"
#. "$HOME/venv/bin/activate"

cd "$HOME/app/webcli"

exec gunicorn \
	--name "webcli" \
	--workers 1 \
	--max-requests 0 \
	--bind "unix:$HOME/run/http" \
	"webcli.wsgi:application"
